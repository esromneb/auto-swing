name: C/C++ CI

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest
    container:
      image: esromneb/arduino:1

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
#    - name: make check
#      run: make check
#    - name: make distcheck
#      run: make distcheck
